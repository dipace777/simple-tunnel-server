syntax = "proto3";

package tunnel;

service TunnelService {
  rpc Connect(stream TunnelRequest) returns (stream TunnelResponse);
}

message TunnelRequest {
  string clientId = 1;
  string token = 2;
}

message TunnelResponse {
  string command = 1;
  string payload = 2;
}
